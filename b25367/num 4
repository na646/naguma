def syracuse_sequence(x):
    sequence = [x]  
    while x != 1:
        if x % 2 == 0:
            x = x // 2  
        else:
            x = 3 * x + 1  
        sequence.append(x)  
    return sequence

initial_number = 7
sequence = syracuse_sequence(initial_number)
print(sequence)
